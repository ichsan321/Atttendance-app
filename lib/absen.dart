import 'package:flutter/material.dart';

class absen {
  String id, email, jam, date, location;

  absen({
    required this.id,
    required this.email,
    required this.jam,
    required this.date,
    required this.location,
  });
}
